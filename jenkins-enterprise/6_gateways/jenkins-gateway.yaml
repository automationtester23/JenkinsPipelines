apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: sumedh-devops-istio-gateway
  namespace: sumedh-devops
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
